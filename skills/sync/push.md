1. New Branch: We cannot push to main so if we r not in a brach we need to make one.

   - Update main
   - Create branch from main. 
     - Name: `<type>-<name>` (type: `skill|agent|cmd|plan|rules`, name: max 3 words no verbs)

2. New PR: We cannot create the PR ourselves cos then we cannot merge it

   - Run /alley-pr

3. Once PR exists u can push the changes
  
4. After changes are push start the watcher again
