---
name: msg
description: Use to communicate with other agents (dudes). Trigger when told to ask, message, tell another dude something.
---

Run `/tell` or `/ask` depending on whether a reply is expected.

- "tell bob ..." or "message bob ..." (no reply needed) → `/tell bob ...`
- "ask bob ..." (reply expected) → `/ask bob ...`
