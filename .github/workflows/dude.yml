name: dude

on:
  workflow_dispatch:
    inputs:
      prompt:
        type: string
        required: true
      version:
        type: string
        required: false
      cwd:
        type: string
        required: false
      timeout:
        type: number
        required: false
      claude_args:
        type: string
        required: false
jobs:
  dude:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "hello"
