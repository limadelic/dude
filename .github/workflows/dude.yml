name: dude

on:
  workflow_dispatch:
    inputs:
      prompt:
        type: string
        required: true
      version:
        type: string
        required: false
      cwd:
        type: string
        required: false
      timeout:
        type: string
        required: false
      claude_args:
        type: string
        required: false
jobs:
  dude:
    runs-on: garm-dev-dlas1-vm-linux-mega-large
    timeout-minutes: ${{ inputs.timeout || 30 }}
    steps:
      - name: Test
        run: echo "hello"
